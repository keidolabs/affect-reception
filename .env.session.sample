# .env.session — active Vast.ai instance config
# Copy to .env.session and fill in values from the Vast.ai console.
# Update at the start of each cloud session. Never commit .env.session.
#
# Usage: make session  (reads these automatically)

INSTANCE=203.0.113.42
PORT=43210

# Default experiment and model for this session (override on command line if needed)
EXP=16_v2_knockout
MODEL=llama8b_inst
