# === SECRETS & CREDENTIALS ===
.env
# .env.session — per-session cloud instance config (IP/port), never commit
.env.session
!.env.sample
!.env.session.sample

# === LARGE BINARY FILES (model weights, raw activations, serialized models) ===
# Raw activation arrays — too large for git, re-generate with extraction scripts
*.pt
*.bin
*.npy
*.npz
*.safetensors
# Serialized scikit-learn models — re-fit from run.py
*.joblib

# === ACCIDENTAL DUPLICATES ===
# "experiments/20_binary_confound_control copy/" — accidental Finder duplicate of exp 20
experiments/20_binary_confound_control copy/

# === PYTHON ===
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
.uv/

# === LOGS ===
*.log
runs.log

# === LOCAL CACHES / GENERATED INDICES ===
# .concepts/ — codebase semantic index auto-generated by Claude Code
.concepts/
# results/ — flat CSV mirror of experiment outputs; re-generate from outputs/
results/

# === CLAUDE CODE ===
.claude/settings.local.json

# === INTERNAL PROJECT MANAGEMENT ===
# PRPs/ — pre-run procedures / internal planning docs
PRPs/

# === OS ===
.DS_Store
.DS_Store?

# === IDE ===
.vscode/
.idea/
